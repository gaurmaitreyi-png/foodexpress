Drop your own screenshots here, named to match the README:
  home.png    - the homepage / restaurant list
  schema.png  - your DB schema diagram (or Django admin showing the models)
  menu.png    - a restaurant detail / menu page
  render.png  - the Render dashboard after a successful deploy
  vercel.png  - the Vercel dashboard after a successful deploy

Take these as you go through deployment so the README shows YOUR real process.
