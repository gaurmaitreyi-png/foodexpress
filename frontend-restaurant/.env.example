# Copy to frontend-restaurant/.env for local dev.
# Backend API base URL — same backend the customer app uses.
# Production (Vercel): set to your Render backend, e.g. https://foodexpress-api.onrender.com/api
VITE_API_URL=http://localhost:8000/api
